# Backend Environment Variables
# Copy this to .env and fill in your values

# How often to poll sensors (seconds)
POLLING_INTERVAL=60

# WeatherFlow Tempest API token (REQUIRED for Tempest weather stations)
# Get one at https://tempestwx.com/settings/tokens
TEMPEST_API_TOKEN=your-tempest-token-here

# =============================================================================
# EMAIL ALERTS CONFIGURATION
# =============================================================================
# Set these to enable email notifications when sensors have errors

# SMTP server settings
#
# Hostinger example (recommended):
#   SMTP_HOST=smtp.hostinger.com
#   SMTP_PORT=465          (SSL is auto-detected on port 465)
#   SMTP_USER=alerts@your-domain.com   (the full Hostinger mailbox address)
#   SMTP_PASSWORD=the mailbox password
#
# Gmail example:
#   SMTP_HOST=smtp.gmail.com
#   SMTP_PORT=587
#   Use an "App Password": https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465

# Force SSL on non-465 ports (rarely needed; 465 auto-enables SSL)
# SMTP_USE_SSL=true

SMTP_USER=your-mailbox@your-domain.com
SMTP_PASSWORD=your-mailbox-password

# Immediate emails when any device goes down or comes back online
# (set to false to only get the 1-hour diagnostic alerts)
STATUS_CHANGE_EMAILS=true

# Who receives the alerts
ALERT_EMAIL=dashboard@oberlin.edu

# Email shown as sender
FROM_EMAIL=sensor-dashboard@oberlin.edu

# Minimum seconds between alerts for the same sensor (default: 300 = 5 minutes)
ALERT_COOLDOWN=300

# Recipients for "new sensor added" notifications (comma-separated; optional -
# defaults are hardcoded in email_service.py)
# NEW_SENSOR_TO=person1@example.com,person2@example.com
# NEW_SENSOR_CC=person3@example.com
