# Backend Environment Variables
# ==============================
# Copy this file to .env and fill in your values

# Server Configuration
HOST=0.0.0.0
PORT=8000

# External Endpoint (where CSV data is uploaded)
# For testing: Use http://localhost:8000/api/test/upload/csv
# For production: Use your real endpoint
# NOTE: Upload tokens are provided per-sensor when adding a sensor, not here
EXTERNAL_ENDPOINT_URL=https://oberlin.communityhub.cloud/api/data-hub/upload/csv

# Polling Configuration
# How often to fetch data from sensors (in seconds)
POLLING_INTERVAL=60

# CORS Configuration
# Frontend URL for CORS (when hosting frontend on Vercel/Netlify, add that URL here)
FRONTEND_URL=http://localhost:5173

# Optional: Logging Level
# LOG_LEVEL=INFO

# Optional: Request Timeout
# REQUEST_TIMEOUT=10
